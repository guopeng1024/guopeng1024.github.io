{
  "name": "guopeng-books",
  "description": "日常的笔记",
  "author": "guopeng@gmail.com",
  "private": true,
  "scripts": {
    "dev": "npx vuepress dev",
    "build": "npx vuepress build",
    "publish": "node ./generateMD.js && npm run build && node ./cp.js && git add . && git commit -m '发布' && git push origin HEAD:main --force"
  },
  "dependencies": {
    "vuepress": "^1.2.0",
    "vuepress-plugin-comment": "^0.7.2"
  },
  "devDependencies": {
    "@vuepress/plugin-back-to-top": "^1.2.0",
    "@vuepress/plugin-nprogress": "^1.2.0",
    "vuepress-auto-sidebar.js": "^1.0.1"
  }
}
